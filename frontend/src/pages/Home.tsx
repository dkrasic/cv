// import { ExperienceItem } from '../components/ExperienceItem'
// import { Section } from '../components/Section'

export const Home = () => {
  return (
    <>
      {/* <Section title="Work experience" description="Some description">
        {data.experiences.map(({ companyName, positions, projects, startDate, endDate }, index) => (
          <ExperienceItem
            key={index}
            companyName={companyName}
            positions={positions}
            projects={projects}
            startDate={startDate}
            endDate={endDate}
          ></ExperienceItem>
        ))}
        data
      </Section> */}
    </>
  )
}
